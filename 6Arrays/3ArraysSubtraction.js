'use strict'
//we can use the key word pop to remove the last element of an array
const countries=[`kenya`,`uganda`,`Tanzania`,`Rwanda`]
/* when the method countries.pop() is Run it removes the last element in the array which
happens to be Rwanda when run again it removes Tanzania and so on*/
countries.pop()
// We can also remove the first element in the Array by using the shift method i.e
const footballTeams =[`Gor Mahia`,`Chingwe`,`Wazito`,`Liverpool`]
footballTeams.shift()
/* when the footballTeams.shift() method is run it removes the first element
which happens to be Gor Mahia and so on
*/ 
